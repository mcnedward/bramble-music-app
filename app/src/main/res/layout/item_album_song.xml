<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/album_song_item"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/song_track"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="15dp"
        android:text="1"/>

    <com.mcnedward.bramble.view.mediaItem.GifView
        android:id="@+id/song_gif_view"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginLeft="5dp"
        android:layout_alignParentLeft="true"
        android:scaleType="fitXY"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/song_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@+id/song_duration"
        android:layout_toRightOf="@id/song_track"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:padding="15dp"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:text="SONG TITLE HERE"/>

    <TextView
        android:id="@+id/song_duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:padding="15dp"
        android:text="5:00"/>

</RelativeLayout>